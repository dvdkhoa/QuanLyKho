@model QuanLyKho.Models.Entities.DetailedConfig
@using QuanLyKho.Models.Entities;

@{
    ViewData["Title"] = "Create";
    ViewData["PrimaryTitle"] = "Detail Configurations";

}



<section class="content">
    <form asp-action="Create" method="post">
        <div class="row">
            <div class="col-md-8 offset-2">
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">Add detail configuration</h3>
                        <div class="card-tools">
                            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                                <i class="fas fa-minus"></i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body" style="display: block;">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="Name">Configuration Name</label>
                            <input asp-for="Name" type="text" class="form-control">
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label>Category</label>
                            @*<select id="select-role" class="form-control custom-select" asp-items="@ViewBag.CategoryId">*@
                            <select id="select-role" class="w-100" name="categories" asp-items="@ViewBag.CategoryId" multiple>
                                <option selected disabled>Select category</option>
                            </select>
                        </div>
                        <input type="hidden" asp-for="Status" value="@Status.Show" />
                        @*<div class="form-group">
                            <label asp-for="Status">Status</label>
                            <select asp-for="Status" class="form-control custom-select">
                                <option selected disabled="">Select one</option>
                                @foreach (Status status in Enum.GetValues(typeof(Status)))
                                {
                                    <option value="@status">@status.ToString()</option>
                                }
                            </select>
                            <span asp-validation-for="Status" class="text-danger"></span>
                        </div>*@

                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>

        </div>
        <div class="row">
            <div class="col-md-8 offset-2">
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                <input type="submit" value="Create" class="btn btn-success float-right">
            </div>
        </div>
    </form>
</section>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/lib/multiple-select/multiple-select.min.js"></script>
    <link href="~/lib/multiple-select/multiple-select.min.css" rel="stylesheet" />
    <script>
        $("#select-role").multipleSelect({
            selectAll: false,
            keepOpen: false,
            isOpen: false,
        });
    </script>
}
